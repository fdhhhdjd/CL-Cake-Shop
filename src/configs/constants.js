// Todo: Node
export const NODE = {
  DEV: 'development',
  PRO: 'production',
};

// Todo: Name Redux
export const REDUX_NAME = {
  CART: 'cart_slice',
  PRODUCT: 'product_slice',
};

// Todo: TIME
export const TIME = {
  _1_SECOND: 1000,
};

// Todo: ROUNDED NUMBER DECIMAL
export const ROUND = {
  NUMBER: 3,
};
